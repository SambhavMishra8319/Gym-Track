export const FOOD_SCHEMA_VERSION = "1.0.0";

export const createFoodRecord = ({
  id,
  name,
  category = "Other",
  source = "local",
  sourceId = null,
  brand = null,
  barcode = null,
  servingSize = 100,
  servingUnit = "g",
  calories = 0,
  protein = 0,
  carbs = 0,
  fat = 0,
  fiber = 0,
  sugar = 0,
  sodium = 0,
  calcium = 0,
  iron = 0,
  potassium = 0,
  aliases = [],
  commonUnits = [{ unit: "g", grams: 1 }],
  verified = false,
}) => ({
  id,
  name,
  category,
  source,
  sourceId,
  brand,
  barcode,
  servingSize,
  servingUnit,
  calories,
  protein,
  carbs,
  fat,
  fiber,
  sugar,
  sodium,
  calcium,
  iron,
  potassium,
  aliases,
  commonUnits,
  verified,
});
